export enum InputFieldType {
  NUMBER = 'number',
  PASSWORD = 'password',
  EMAIL = 'email',
  TEXTAREA = 'textarea',
  TEL = 'tel',
  DATE = 'date',
  SELECT = 'select',
  CHECKBOX = 'checkbox',
  TEXT = 'text',
  SEARCH = 'search',
  RADIO = 'radio',
}

export enum Variant {
  OUTLINED = 'outlined',
  STANDARD = 'standard',
  FILLED = 'filled',
}

export enum Margin {
  NONE = 'none',
  NORMAL = 'normal',
  DENSE = 'dense',
}
